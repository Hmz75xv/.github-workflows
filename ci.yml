name: CI/CD

on:
  push:
    branches:
      - main

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Print Hello
        run: echo "Hello world!"


- name: Checkout code
  uses: actions/checkout@v4
